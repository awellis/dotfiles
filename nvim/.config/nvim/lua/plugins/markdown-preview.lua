-- Use the shared prose-preview command instead. This keeps Markdown rendering,
-- math handling, and styling identical across Helix and Neovim.
return {
  {
    "iamcco/markdown-preview.nvim",
    enabled = false,
  },
}
